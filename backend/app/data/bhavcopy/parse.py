"""T3 — Parse layer: parse legacy + UDiFF day files into the unified raw row
schema (isin, symbol, date, open, high, low, close, volume, traded_value,
series). Not yet implemented (see specs/v2/01_DATA_LAYER_TASKS.md T3)."""


def parse_file(*args, **kwargs):
    raise NotImplementedError("T3: parse layer not yet implemented")
